SELECT *, count(*) as users, SUM(employees.salary) as total_salary_per_department from departments LEFT JOIN employees on employees.department_id = departments.department_id GROUP BY employees.department_id HAVING total_salary_per_department > 200000;